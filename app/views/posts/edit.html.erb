
<h1>Edit post</h1>

<%= render 'form' %>
<p><%= link_to 'Edit', edit_post_path(@post) %> | <%= link_to 'Destroy', post_path(@post),
        method: :delete,
        data: { confirm: 'Are you sure?' } %></p>
<%= link_to 'Back', posts_path %>

<!-- <%= link_to 'New post', new_post_path %> -->
<h1>Ultracrepidate</h1>
<% @posts.map.uniq.each do |u| %>
  <h2><%= link_to u.title, post_path(u) %></h2>
  <%= u.text %>
  <!-- <p><%= link_to 'Edit', edit_post_path(u) %> | <%= link_to 'Destroy', post_path(u),
          method: :delete,
          data: { confirm: 'Are you sure?' } %></p> -->
<% end %>


<!-- <h1>Listing Posts</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Text</th>
  </tr>

  <% @posts.each do |post| %>
    <tr>
      <td><%= post.title %></td>
      <td><%= post.text %></td>
      <td><%= link_to 'Show', post_path(post) %></td>
      <td><%= link_to 'Edit', edit_post_path(post) %></td>
      <td><%= link_to 'Destroy', post_path(post),
              method: :delete,
              data: { confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %>
</table> -->
